{% extends '@layout/default.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="page">
        <div class="page-header">
            <h1 class="page-title title">{{ page.title }}</h1>
        </div>
        <div class="page-body mt-4">{{ page.body|markdown_to_html }}</div>
    </div>
{% endblock %}